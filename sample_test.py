
def sum_of_list_elements(lst):
    sum_elements = sum(lst)
    return sum_elements

# Example list
numbers = [1, 2, 3, 4, 5]

# Calculate sum of elements in the list
total_sum = sum_of_list_elements(numbers)
print("Sum of all elements in the list:", total_sum)
